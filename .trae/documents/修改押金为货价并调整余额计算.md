# 修改押金为货价并调整余额计算

## 修改内容

### 1. calculator.ts 修改计算逻辑
- 修改 `calculateTotalPaid` 函数，将 `otherFees` 加入计算
- 新逻辑：已付总额 = 货价 + 其他费用 + 还款

### 2. RentalList.tsx 修改显示
- 将"押金"列标题改为"货价"
- 将"押金总额"统计卡片改为"货价总额"

## 新的计算公式
- **应付总额** = 租金
- **已付总额** = 货价 + 其他费用 + 还款
- **余额** = 应付总额（租金） - 已付总额（货价 + 其他费用 + 还款）