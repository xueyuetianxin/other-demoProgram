# 修改应付总额计算逻辑

## 当前逻辑
`calculateTotalDue` 计算的是：租金 + 其他费用

## 新逻辑
将 `calculateTotalDue` 改为只返回租金，不包含其他费用。

## 修改内容
在 `calculator.ts` 中修改 `calculateTotalDue` 函数，直接返回 `calculateRent(rental)`，不再加上 `rental.otherFees`。