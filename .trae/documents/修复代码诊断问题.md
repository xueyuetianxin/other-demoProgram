# 修复代码诊断问题

## 问题 1：RentalForm.tsx 类型错误
在 `addRental` 调用中添加 `payments: []` 属性，因为 `Rental` 接口现在需要 `payments` 数组。

## 问题 2：calculator.ts 未使用的导入
移除未使用的 `Payment` 导入。

## 修复内容
1. 在 RentalForm.tsx 的 `handleSubmit` 中添加 `payments: []`
2. 在 calculator.ts 中移除 `Payment` 导入