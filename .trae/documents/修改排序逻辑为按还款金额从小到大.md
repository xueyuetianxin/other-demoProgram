# 修改租赁列表排序逻辑

## 当前排序逻辑
- 先按结算状态排序（未结清在前）
- 结算状态相同时，按创建时间排序（新的在前）

## 新的排序逻辑
- 先按结算状态排序（未结清在前）
- 结算状态相同时，按还款状态金额从小到大排序
  - 按天计租：按当日已还金额排序
  - 按月计租：按当月已还金额排序

## 修改内容
在 `RentalList.tsx` 中修改 `sortedRentals` 的排序逻辑，使用 `calculateDailyPaid` 或 `calculateMonthlyPaid` 来获取还款金额，然后按金额从小到大排序。