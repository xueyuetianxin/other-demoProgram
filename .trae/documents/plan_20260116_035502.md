# 修复 iOS IPA 导出失败问题

## 问题分析
当前使用 `iphonesimulator` SDK 构建，生成的是模拟器存档，无法导出为 IPA 文件。

## 解决方案
1. 修改构建命令，使用 `iphoneos` SDK 代替 `iphonesimulator`
2. 简化 exportOptions.plist 配置，移除不必要的团队 ID
3. 更新 GitHub Actions 工作流

## 执行步骤
1. 更新 exportOptions.plist 配置
2. 修改构建命令，使用 `iphoneos` SDK
3. 更新工作流文件
4. 提交并推送更改