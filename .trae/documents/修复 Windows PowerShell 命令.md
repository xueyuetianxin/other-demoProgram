# 修复 Windows PowerShell 命令

## 问题分析
用户尝试使用 Linux 命令 `rm -rf`，但在 Windows PowerShell 中不工作。

## 解决方案
提供正确的 Windows PowerShell 命令来清除 node_modules 和 package-lock.json。

## 执行步骤
1. 使用 Windows PowerShell 命令清除 node_modules
2. 使用 Windows PowerShell 命令删除 package-lock.json
3. 重新安装依赖